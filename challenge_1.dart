// Note: main is the entry point of all Dart programs

void main() {
  /*  Create a variable called `enterpriseShields` and set it equal to 100
   *  Create an integer variable called `defiantShields` and set to 80
   *  Print both values out
   *  Create a boolean variable called `dominantShip` that compares the first 
   *  two variables and sets to true if enterpriseShields are greater than 
   *  defiantShields
   */

  var enterpriseShields = 100;
  int defiantShields = 80;
  print(enterpriseShields);
  print(defiantShields);

  bool dominantShip = enterpriseShields > defiantShields;
  print(dominantShip);
}
